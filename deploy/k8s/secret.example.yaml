# Example only — DO NOT COMMIT real values. Replace placeholders and apply via
#   kubectl apply -f deploy/k8s/secret.yaml
# Or wire to an external secret manager (e.g. AWS Secrets Manager + ESO).
apiVersion: v1
kind: Secret
metadata:
  name: easypay-secrets
type: Opaque
stringData:
  DB_DSN: "user:pass@tcp(mysql.svc:3306)/payments?parseTime=true&loc=UTC"
  REDIS_ADDR: "redis.svc:6379"
  REDIS_PASSWORD: ""
  KAFKA_BROKERS: "kafka-0.kafka.svc:9092,kafka-1.kafka.svc:9092"
  STRIPE_SECRET_KEY: "sk_live_REPLACE_ME"
  STRIPE_WEBHOOK_SECRET: "whsec_REPLACE_ME"
  HMAC_SECRET: "REPLACE_WITH_32_BYTE_RANDOM"
  ETH_RPC_WS: "wss://example/v2/REPLACE"
  ETH_RPC_HTTP: "https://example/v2/REPLACE"
  ETH_CONTRACT_ADDRESS: "0x0000000000000000000000000000000000000000"
  ETH_CHAIN_ID: "1"
